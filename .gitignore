# Python / uv
.venv/
__pycache__/
*.pyc
.ruff_cache/

# Local secrets — .env.example IS committed, .env is NOT
.env

# OS / editors
.DS_Store
.idea/
.vscode/

# data/out is scratch (per-query dumps); keep the folder, ignore its contents.
data/out/*
!data/out/.gitkeep

# COMMITTED on purpose:
#   - data/in/  (your chosen PDF — the organisers check answers against it)
#   - submission/  (your team.json + the nine answers — your deliverable)

CLAUDE.md

playground/